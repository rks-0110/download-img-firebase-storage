# import-and-export-firebase-rtdb&storage
python project to either import or export JSON data to/from it and also export JPEG files from firebase storage according to the JSON files previously exported
